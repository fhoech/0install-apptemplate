#!/bin/sh
shopt -s execfail
DIR=`dirname "$0"`
if [ -x "$DIR/com.apple.Python2.6" ]; then
	exec arch -i386 "$DIR/com.apple.Python2.6" "$@"
elif [ -x "$DIR/org.python.Python2.6" ]; then
	exec "$DIR/org.python.Python2.6" "$@"
fi
exit 1
