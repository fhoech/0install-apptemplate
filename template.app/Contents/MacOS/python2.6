#!/bin/sh
shopt -s execfail
DIR=`dirname "$0"`
if [ -x "$DIR/com.apple.Python2.7" ]; then
	exec arch -i386 "$DIR/com.apple.Python2.7" "$@"
elif [ -x "$DIR/org.python.Python2.7" ]; then
	exec "$DIR/org.python.Python2.7" "$@"
fi
exit 1
